# Gate 2 Cut — 2026-06-05 (D4 EOD, ~1.5 days ahead of schedule)

## Acceptance criteria (from plan)

> Spanner ingestion live for `02` vs `03`. `agent_runs` table populated on
> every real-LLM call. Reflector successfully re-queries Spanner on
> injected low-confidence. Offline path green.

## Verification

### 1. Offline stub regression — clean
```
$ python -m app.chain
amendment=AMD-2026-capital-adequacy | clauses=4 | obligations=6 | matches=6 (missing=1) | diffs=5 | priority=medium
```
Canonical line preserved exactly.

### 2. Spanner ingestion live — 02 + 03 round-trip
```
$ CURATOR_LIVE_GCP=1 CURATOR_DOCAI_PROCESSOR_ID=… pytest tests/integration/test_pipeline.py tests/integration/test_docai.py -v
tests/integration/test_pipeline.py::test_ingest_two_pdfs_dry_run PASSED
tests/integration/test_pipeline.py::test_spanner_graph_backend_round_trip PASSED
tests/integration/test_docai.py::test_parse_02_amendment_smoke PASSED
============================== 3 passed in 54.82s ==============================
```

- DocAI Layout Parser parses `data/fixtures/source_pdfs/02_second_amendment_*.pdf` cleanly (≥5 chunks; heading-typed; sensible page numbers).
- `pipeline.ingest_two_pdfs(02, 03, namespace, dry_run=True)` produces an `AgentState` with populated `amendment`, `amended_clauses` (≥3), and `policies` (≥3 from `MockGroundingBackend.load_bank_policies` fallback per D5 deferral).
- `SpannerGraphBackend.ingest_document` + `get_master_direction_text` + `search_master_direction` + `query_graph_neighborhood` all round-trip a synthetic 3-clause document; cleanup via DML DELETE.

### 3. agent_runs populating live — 62 rows from one chain run

```
$ source scripts/env.sh
$ CURATOR_REAL_LLM=1 CURATOR_AGENT_RUN_LOG=1 python -m app.chain
amendment=AMD-2026-capital-adequacy | clauses=4 | obligations=21 | matches=21 (missing=7) | diffs=19 | priority=critical

$ gcloud spanner databases execute-sql curator --instance=curator-graph … \
    --sql="SELECT agent_name, lens, COUNT(*) AS n, AVG(latency_ms) AS avg_latency_ms ..."

agent_name             lens                   n   avg_latency_ms
auditor_lens           auditor_lens           4   5480.75
banker_lens            banker_lens            4   4954.50
compliance_lens        compliance_lens        4   6266.50
customer_protect_lens  customer_protect_lens  4   6033.50
diff_agent             None                   19  5405.42
judge_icaap_critic     judge_icaap_critic     1   8842.00
judge_impact_critic    judge_impact_critic    1   8389.00
judge_ops_risk_critic  judge_ops_risk_critic  1   9109.00
judge_pillar3_critic   judge_pillar3_critic   1   6693.00
map_agent              None                   21  4439.05
qna_agent              None                   2   4825.50
```

- 4 lenses × 4 clauses = **16 decompose-panel rows** ✓
- 4 critics × 1 judge invocation = **4 judge-panel rows** ✓
- 21 + 19 + 2 = **42 single-agent rows** (map / diff / qna) ✓
- Total **62 rows** for one chain run.
- `lens` column populated for panel members, NULL for single-agent calls. Latencies in 4-9 s/call range — sensible.
- `pipeline_run_id` propagates correctly across `ThreadPoolExecutor` workers (Track 2's `ContextVar` + module mirror — verified by `test_log_run_threadlocal_propagation` and now by the live row clustering).

### 4. Reflector re-query — verified at unit + live boundary

- 4 unit tests in `tests/integration/test_reflector_requery.py` pass:
  - high-confidence no-op
  - low-confidence calls `grounding.query_graph_neighborhood` with correct args (mocked backend)
  - graceful degrade when grounding raises
  - graceful degrade when method missing
- `SpannerGraphBackend.query_graph_neighborhood` exists and round-trips live (covered inside `test_spanner_graph_backend_round_trip`).
- Wiring into the demo path is the D5 integration step; the Python function `reflect_and_requery` is callable today.

### 5. Test suite
`pytest tests/unit tests/integration -q`: **12 passed, 11 skipped, 3 pre-existing failures** in `test_server_e2e.py` (404s on `/chat_stream` and `/collect_feedback` — those endpoints will be added in D5 when `app/fast_api_app.py` is extended for the Discovery Inbox UI).

## Architecture state at Gate 2

```
                         ┌─────────────────────────────┐
   PDF inputs            │  app/ingest/pipeline.py     │
   (Cloud Run upload     │  ingest_two_pdfs(a, b, ns)  │
    or fixture file)     └──────────────┬──────────────┘
                                        │
                ┌───────────────────────┼────────────────────────┐
                ▼                       ▼                        ▼
   ┌────────────────────┐  ┌─────────────────────┐   ┌──────────────────────┐
   │  app/ingest/docai  │  │ app/ingest/graph_   │   │ app/grounding.py     │
   │   parse_pdf(pdf)   │  │   extractor.extract │   │ SpannerGraphBackend  │
   │   Doc AI Layout    │  │  (deterministic     │   │  .ingest_document    │
   │   Parser (us)      │  │   clause boundaries)│   │  ...                 │
   └────────────────────┘  └─────────────────────┘   └────────┬─────────────┘
                                                              │
                                                              ▼
                                                 ┌────────────────────────────┐
                                                 │ Spanner (asia-south1):     │
                                                 │  documents / clauses /     │
                                                 │  obligations / agent_runs /│
                                                 │  discovered_items / …      │
                                                 └────────────────────────────┘

   real_decompose / real_map / real_diff / real_judge / real_qna
      └─→ app/runners.py::run_agent (wraps every call with log_run)
          └─→ app/observability/run_log.py
              └─→ Spanner agent_runs (ContextVar-tagged pipeline_run_id)

   Reflector (Python) → app/grounding.get_grounding_backend()
                       .query_graph_neighborhood(node_id, depth, requeries)
                       → Spanner lexical search (Phase-5: vector retrieval)
```

## Commits on stage-2 since Gate 1

| commit | description |
|---|---|
| `ffb3b2f` | phase-2.3 — SpannerGraphBackend + agent_runs writer + Reflector re-query |

## Cost discipline
- D4 (this gate): chain + logging ($0.66) + DocAI on 02 (~$1) + Spanner round-trip (~$0.01) + agent context = ~$3
- **Total Stage-2 spend so far: ~$7 of $200 cap.** Massively under budget.

Spanner instance left running for D5. Tear down command: `bash scripts/spanner_down.sh`.

## Promotion to main
After Gate 2 acceptance, this commit + Gate 2 doc merges into `main` via `git merge --no-ff stage-2` and gets tagged `gate-2`.
